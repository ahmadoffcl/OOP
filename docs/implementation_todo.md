# SCMS Implementation TODO

Use this as a simple step-by-step list.

## Phase 1
- Read `Person.h`, `Student.h`, `Faculty.h`, and `Staff.h`.
- Change names, roll numbers, CNICs, and sample values in `main.cpp`.
- Add one more student yourself and display it through `Person*`.
- Explain why `displayInfo()` is virtual.

## Phase 2
- Read `LibraryItem.h`, `Book.h`, `Journal.h`, and `Library.h`.
- Add one more book in `showLibraryDemo()` by yourself.
- Try searching for a title that does not exist and print a message.
- Read `FeeRecord.cpp` and explain why the copy constructor is written.
- Add one more invoice item by yourself.

## Phase 3
- Read `Accommodation.h` and `Reportable.h`.
- Explain why `HostelManager` inherits from both classes.
- Add one more room in `HostelManager.cpp`.
- Add one more student and allocate that student to hostel.
- Generate `data/campus_report.txt` from the menu.

## Viva Practice
- Be able to explain every class.
- Be able to add a new menu option.
- Be able to add one new getter/setter.
- Be able to explain where inheritance, polymorphism, and exception handling are used.
